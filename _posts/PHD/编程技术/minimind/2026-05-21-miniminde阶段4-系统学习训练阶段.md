---
layout: post
title: miniminde阶段4-系统学习训练阶段
date: 2026-05-21 21:48 +0800
description: 这一阶段gpt有点不给力了，设计的比较差
pin: true
math: true
mermaid: true
category:
- LLM
- minimind
tags:
- LLM
- minimind
published: true
sitemap: false
author: tangyu
---


这一阶段需要覆盖 Pretrain → SFT → LoRA → DPO → PPO/RLAIF → Tool Use / Agentic RL。下面是详细安排

# 第 4 阶段：系统学习训练阶段（1–2 周）

## 总目标

* 理解 LLM 从 base model 到 chat model 的训练全过程
* 熟悉每个训练阶段的输入输出、loss 计算和优化流程
* 能独立运行每个训练模块，并记录实验数据
* 为第 5 阶段科研/实验打好基础

---

## Day 1：Pretrain 数据准备和加载

* **任务**：理解 MiniMind 的 pretrain 数据结构和加载方式

完成 MiniMind 的预训练语料下载、目录结构搭建、数据格式理解，并成功加载数据进入训练数据集类（例如 `PretrainDataset`），为明天的小样本 pretrain 实验做准备。

* **操作**：
  1. 查看 `train/pretrain.py`、`dataset/tokenize_dataset.py`
  2. 读懂 tokenizer 如何处理文本、代码、公式
  3. 小数据实验：用 1–2k 条文本跑 tokenization
  4. 输出 token 数量统计和长度分布

```python
class PretrainDataset(Dataset):
    def __init__(self, data_path, tokenizer, max_length=512):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.samples = load_dataset('json', data_files=data_path, split='train')

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, index):
        sample = self.samples[index]
        #一条一条的取出来
        tokens = self.tokenizer(str(sample['text']), add_special_tokens=False, max_length=self.max_length - 2, truncation=True).input_ids
        #拼接开始和结束字符
        tokens = [self.tokenizer.bos_token_id] + tokens + [self.tokenizer.eos_token_id]
        #pad长度
        input_ids = tokens + [self.tokenizer.pad_token_id] * (self.max_length - len(tokens))
        #转为tensor
        input_ids = torch.tensor(input_ids, dtype=torch.long)
        labels = input_ids.clone()
        #pad_token_id占位-100，-100意味着损失函数跳过这些位置
        labels[input_ids == self.tokenizer.pad_token_id] = -100
        return input_ids, labels
```

这个没啥的，可以忽略很多内容，第3阶段已经做了很多了，今天就是tokenization的运用和vocab的查看。

```
📌 今日思考与提升问题（建议记录）

为什么 pretrain 数据只包含单 field text
- 因为目标是 next token 预测（causal LM），不需要对话结构。
为什么要以 JSONL 而不是普通文本？
- 便于结构化、可包含 metadata，并统一不同数据来源。
如何理解 “next token prediction” 与 attention mask?
- 模型的 training loss 使用因果掩码，使其只能看到左侧 tokens。
你估计 pretrain_t2t_mini.jsonl 中平均 token/行是多少？
- 这影响 batch 和 seq_len 的选择
```

训练的时候，其实是把value的数据拿出来拼入首尾符号再训练。



### 分词器训练

{% raw %}

```python
# 注：不建议再重复训练tokenizer（“词典”），MiniMind已自带，此脚本仅供学习和参考。基于不同词典训练的模型将导致输出完全不统一，降低社区的模型复用性
# Note: It is not recommended to re-train the tokenizer. MiniMind already includes one. This script is for learning and reference only. Training models with different tokenizers will lead to inconsistent outputs and reduce model reusability in the community.
import os
import json
from tokenizers import decoders, models, pre_tokenizers, trainers, Tokenizer

DATA_PATH = '../dataset/sft_t2t_mini.jsonl'
TOKENIZER_DIR = '../model_learn_tokenizer/'
VOCAB_SIZE = 6400
SPECIAL_TOKENS_NUM = 36


def get_texts(data_path):  # 数据读取
    with open(data_path, 'r', encoding='utf-8', errors='ignore') as f:
        for i, line in enumerate(f):
            if i >= 10000: break  # 选10000行测试
            try:
                data = json.loads(line)
                contents = [item.get('content') for item in data.get('conversations', []) if item.get('content')]
                if contents:
                    yield "\n".join(contents)  # 使用了生成器（yield），这样可以流式加载数据，避免一次性将大文本读入内存导致 OOM。
            except json.JSONDecodeError:
                continue


def train_tokenizer(data_path, tokenizer_dir, vocab_size, special_tokens_num=SPECIAL_TOKENS_NUM):
    tokenizer = Tokenizer(
        models.BPE())  # 采用 BPE (Byte-Pair Encoding) 算法作为核心分词模型，这是目前 LLM（如 GPT-4, Llama, Qwen 等）最常用的算法。
    # ByteLevel 预分词：在真正进行 BPE 合并前，先把文本按字节级别处理。它的好处是：词表中不需要保留所有 Unicode 字符（比如生僻字），遇到未知的字也能用字节拼出来，天然支持多语言。add_prefix_space=False 表示不在句子开头强行加空格。
    tokenizer.pre_tokenizer = pre_tokenizers.ByteLevel(add_prefix_space=False)

    special_tokens_list = [  # 特殊token
        "<|endoftext|>", "<|im_start|>", "<|im_end|>",
        "<|object_ref_start|>", "<|object_ref_end|>", "<|box_start|>", "<|box_end|>", "<|quad_start|>", "<|quad_end|>",
        "<|vision_start|>", "<|vision_end|>", "<|vision_pad|>", "<|image_pad|>", "<|video_pad|>",
        "<|audio_start|>", "<|audio_end|>", "<|audio_pad|>", "<tts_pad>", "<tts_text_bos>", "<tts_text_eod>",
        "<tts_text_bos_single>"
    ]

    additional_tokens_list = [  # 额外的token，如工具链调用
        "<tool_call>", "</tool_call>",
        "<tool_response>", "</tool_response>",
        "<think>", "</think>"
    ]
    # SPECIAL_TOKENS_NUM = 36
    num_buffer = special_tokens_num - len(special_tokens_list + additional_tokens_list)  # 预留一些token，
    # 预留了 13 个 <|buffer1|>, <|buffer2|> … 占位符。这是一种前瞻性设计：如果未来需要新增特殊 token，可以在这些预留位上替换，而不会改变已有 token 的 ID 编号，从而保证旧模型的兼容性。
    buffer_tokens = [f"<|buffer{i}|>" for i in range(1, num_buffer + 1)]  # 预留一定数量的token位置
    all_special_tokens = special_tokens_list + additional_tokens_list + buffer_tokens  # 等于SPECIAL_TOKENS_NUM
    trainer = trainers.BpeTrainer(
        vocab_size=vocab_size,  # 总词表大小
        show_progress=True,  # 进度条
        # 有256个字节作为初始词表，将任何token可以分解为字节，确保任何语言的文本都能被分解到字节级别，不会出现 OOV（Out-of-Vocabulary，词表外无法编码）的问题。
        initial_alphabet=pre_tokenizers.ByteLevel.alphabet(),
        special_tokens=all_special_tokens  # 优先将36个特殊token编入词表最前面
    )
    texts = get_texts(data_path)  # 流式读取，jsonl数据集，提取对话文本
    tokenizer.train_from_iterator(texts, trainer=trainer)  # BPE 算法会不断统计相邻 token 对的频率，将最高频的合并为新 token，直到词表达到 6400。
    tokenizer.decoder = decoders.ByteLevel()  # 设置解码器
    tokenizer.add_special_tokens(special_tokens_list)  # 添加特殊字符

    os.makedirs(tokenizer_dir, exist_ok=True)
    tokenizer.save(os.path.join(tokenizer_dir, "tokenizer.json"))  # 词表
    tokenizer.model.save(tokenizer_dir)
    tokenizer_json_path = os.path.join(tokenizer_dir, "tokenizer.json")
    with open(tokenizer_json_path, 'r', encoding='utf-8') as f:
        tokenizer_data = json.load(f)
    for token_info in tokenizer_data.get('added_tokens', []):
        if token_info['content'] not in special_tokens_list:
            token_info['special'] = False
    with open(tokenizer_json_path, 'w', encoding='utf-8') as f:
        json.dump(tokenizer_data, f, ensure_ascii=False, indent=2)

    added_tokens_decoder = {}
    for i, token in enumerate(all_special_tokens):
        idx = tokenizer.token_to_id(token)
        added_tokens_decoder[str(idx)] = {
            "content": token,
            "lstrip": False,
            "normalized": False,
            "rstrip": False,
            "single_word": False,
            "special": True if token in special_tokens_list else False
        }

    config = {
        "add_bos_token": False,
        "add_eos_token": False,
        "add_prefix_space": False,
        "added_tokens_decoder": added_tokens_decoder,
        "additional_special_tokens": [t for t in special_tokens_list if t not in ["<|endoftext|>"]],
        "bos_token": "<|im_start|>",
        "clean_up_tokenization_spaces": False,
        "eos_token": "<|im_end|>",
        "legacy": True,
        "model_max_length": 131072,
        "pad_token": "<|endoftext|>",
        "sp_model_kwargs": {},
        "spaces_between_special_tokens": False,
        "unk_token": "<|endoftext|>",
        "image_token": "<|image_pad|>",
        "audio_token": "<|audio_pad|>",
        "video_token": "<|video_pad|>",
        "vision_bos_token": "<|vision_start|>",
        "vision_eos_token": "<|vision_end|>",
        "audio_bos_token": "<|audio_start|>",
        "audio_eos_token": "<|audio_end|>",
        "chat_template": "{%- if tools %}\n    {{- '<|im_start|>system\\n' }}\n    {%- if messages[0].role == 'system' %}\n        {{- messages[0].content + '\\n\\n' }}\n    {%- endif %}\n    {{- \"# Tools\\n\\nYou may call one or more functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\n<tools>\" }}\n    {%- for tool in tools %}\n        {{- \"\\n\" }}\n        {{- tool | tojson }}\n    {%- endfor %}\n    {{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\n</tool_call><|im_end|>\\n\" }}\n{%- else %}\n    {%- if messages[0].role == 'system' %}\n        {{- '<|im_start|>system\\n' + messages[0].content + '<|im_end|>\\n' }}\n    {%- endif %}\n{%- endif %}\n{%- set ns = namespace(multi_step_tool=true, last_query_index=messages|length - 1) %}\n{%- for message in messages[::-1] %}\n    {%- set index = (messages|length - 1) - loop.index0 %}\n    {%- if ns.multi_step_tool and message.role == \"user\" and message.content is string and not(message.content.startswith('<tool_response>') and message.content.endswith('</tool_response>')) %}\n        {%- set ns.multi_step_tool = false %}\n        {%- set ns.last_query_index = index %}\n    {%- endif %}\n{%- endfor %}\n{%- for message in messages %}\n    {%- if message.content is string %}\n        {%- set content = message.content %}\n    {%- else %}\n        {%- set content = '' %}\n    {%- endif %}\n    {%- if (message.role == \"user\") or (message.role == \"system\" and not loop.first) %}\n        {{- '<|im_start|>' + message.role + '\\n' + content + '<|im_end|>' + '\\n' }}\n    {%- elif message.role == \"assistant\" %}\n        {%- set reasoning_content = '' %}\n        {%- if message.reasoning_content is string %}\n            {%- set reasoning_content = message.reasoning_content %}\n        {%- else %}\n            {%- if '</think>' in content %}\n                {%- set reasoning_content = content.split('</think>')[0].rstrip('\\n').split('<think>')[-1].lstrip('\\n') %}\n                {%- set content = content.split('</think>')[-1].lstrip('\\n') %}\n            {%- endif %}\n        {%- endif %}\n        {%- if true %}\n            {{- '<|im_start|>' + message.role + '\\n<think>\\n' + reasoning_content.strip('\\n') + '\\n</think>\\n\\n' + content.lstrip('\\n') }}\n        {%- endif %}\n        {%- if message.tool_calls %}\n            {%- for tool_call in message.tool_calls %}\n                {%- if (loop.first and content) or (not loop.first) %}\n                    {{- '\\n' }}\n                {%- endif %}\n                {%- if tool_call.function %}\n                    {%- set tool_call = tool_call.function %}\n                {%- endif %}\n                {{- '<tool_call>\\n{\"name\": \"' }}\n                {{- tool_call.name }}\n                {{- '\", \"arguments\": ' }}\n                {%- if tool_call.arguments is string %}\n                    {{- tool_call.arguments }}\n                {%- else %}\n                    {{- tool_call.arguments | tojson }}\n                {%- endif %}\n                {{- '}\\n</tool_call>' }}\n            {%- endfor %}\n        {%- endif %}\n        {{- '<|im_end|>\\n' }}\n    {%- elif message.role == \"tool\" %}\n        {%- if loop.first or (messages[loop.index0 - 1].role != \"tool\") %}\n            {{- '<|im_start|>user' }}\n        {%- endif %}\n        {{- '\\n<tool_response>\\n' }}\n        {{- content }}\n        {{- '\\n</tool_response>' }}\n        {%- if loop.last or (messages[loop.index0 + 1].role != \"tool\") %}\n            {{- '<|im_end|>\\n' }}\n        {%- endif %}\n    {%- endif %}\n{%- endfor %}\n{%- if add_generation_prompt %}\n    {{- '<|im_start|>assistant\\n' }}\n    {%- if open_thinking is defined and open_thinking is true %}\n        {{- '<think>\\n' }}\n    {%- else %}\n        {{- '<think>\\n\\n</think>\\n\\n' }}\n    {%- endif %}\n{%- endif %}",
        "tokenizer_class": "PreTrainedTokenizerFast"
    }

    with open(os.path.join(tokenizer_dir, "tokenizer_config.json"), "w", encoding="utf-8") as f:
        json.dump(config, f, ensure_ascii=False, indent=4)
    print("Tokenizer training completed.")


def eval_tokenizer(tokenizer_dir):
    from transformers import AutoTokenizer
    tokenizer = AutoTokenizer.from_pretrained(tokenizer_dir)
    messages = [
        {"role": "system", "content": "你是一个优秀的聊天机器人，总是给我正确的回应！"},
        {"role": "user", "content": '你来自哪里？'},
        {"role": "assistant", "content": '我来自月球'},
        {"role": "user", "content": '你到底来自哪里？'},
        {"role": "assistant", "content": '我来自地球'}
    ]
    new_prompt = tokenizer.apply_chat_template(
        messages,
        tokenize=False
    )
    print('-' * 100)
    print(new_prompt)
    print('-' * 100)
    print('tokenizer词表长度：', len(tokenizer))
    model_inputs = tokenizer(new_prompt)
    print('encoder长度：', len(model_inputs['input_ids']))
    response = tokenizer.decode(model_inputs['input_ids'], skip_special_tokens=False)
    print('decoder一致性：', response == new_prompt, "\n")
    print('-' * 100)
    print('压缩率测试（Chars/Tokens）：')
    test_texts = [
        # 中文样本 (约200字)
        "人工智能是计算机科学的一个分支，它企图了解智能的实质，并生产出一种新的能以人类智能相似的方式做出反应的智能机器，该领域的研究包括机器人、语言识别、图像识别、自然语言处理和专家系统等。人工智能从诞生以来，理论和技术日益成熟，应用领域也不断扩大，可以设想，未来人工智能带来的科技产品，将会是人类智慧的“容器”。人工智能可以对人的意识、思维的信息过程的模拟。人工智能不是人的智能，但能像人那样思考、也可能超过人的智能。",
        "星际航行是指在星系内甚至星系间的空间中进行的航行。由于宇宙空间极其广阔，传统的化学火箭动力在恒星间航行时显得力不从心。科学家们提出了多种方案，包括离子推进器、核热火箭、甚至是利用反物质作为能源的设想。此外，曲率驱动和虫洞旅行等科幻概念也在理论物理研究中被反复探讨。尽管目前人类的足迹仅限于月球，但随着核聚变技术和材料科学的突破，前往火星乃至更遥远的太阳系边缘将成为可能。",
        # 英文样本 (约200词/字符)
        "Large language models (LLMs) are a type of artificial intelligence (AI) trained on vast amounts of text data to understand and generate human-like language. These models use deep learning techniques, specifically transformers, to process and predict the next word in a sequence. LLMs like GPT-4, Llama, and Claude have demonstrated remarkable capabilities in coding, translation, and creative writing. However, they also face challenges such as hallucinations, where the model generates factually incorrect information, and the need for significant computational resources.",
        "The development of sustainable energy is crucial for the future of our planet. As climate change continues to impact global weather patterns, transitioning from fossil fuels to renewable sources like solar, wind, and hydroelectric power has become an urgent priority. Innovations in battery storage technology and smart grid management are essential to ensure a reliable energy supply. International cooperation and policy frameworks are also necessary to drive the global shift towards a greener economy and reduce carbon emissions.",
        # 混合样本
        "Python 是一种高级编程语言，以其简洁的语法和强大的生态系统而闻名。It is widely used in data science, machine learning, and web development. 开发者可以利用 NumPy, Pandas, and PyTorch 等库快速构建复杂的应用。学习 Python 的过程非常愉快，因为它的代码读起来就像英语一样。Whether you are a beginner or an expert, Python offers something for everyone.",
    ]

    total_compression = 0
    for i, text in enumerate(test_texts):
        encoded = tokenizer.encode(text)
        token_count = len(encoded)
        char_count = len(text)
        compression_ratio = char_count / token_count
        total_compression += compression_ratio
        print(f"样本 {i + 1} | 字符数: {char_count:4} | Tokens: {token_count:3} | 压缩率: {compression_ratio:.2f}")

    print(f"平均压缩率: {total_compression / len(test_texts):.2f}")
    print('-' * 100)
    print('流式解码（字节缓冲）测试：')
    input_ids = model_inputs['input_ids']
    token_cache = []
    for tid in input_ids:
        token_cache.append(tid)
        current_decode = tokenizer.decode(token_cache)
        if current_decode and '\ufffd' not in current_decode:
            display_ids = token_cache[0] if len(token_cache) == 1 else token_cache
            raw_tokens = [tokenizer.convert_ids_to_tokens(int(t)) for t in
                          (token_cache if isinstance(token_cache, list) else [token_cache])]
            print(f'Token ID: {str(display_ids):15} -> Raw: {str(raw_tokens):20} -> Decode Str: {current_decode}')
            token_cache = []


if __name__ == '__main__':
    train_tokenizer(DATA_PATH, TOKENIZER_DIR, VOCAB_SIZE)
    eval_tokenizer(TOKENIZER_DIR)

```

{% endraw %}

核心就是评估分词器的压缩率，压缩率越大说明分词效果越好，信息压缩程度越高。
 BPE (Byte-Pair Encoding) 256然后组成6400个词，大概了解下分词的过程。

## Day 2：Pretrain 小规模训练实验

* **任务**：跑通 Pretrain 主线
* **操作**：

  1. 配置小 batch size、seq_len、梯度累积
  2. 跑 100–500 step，观察 loss 曲线
  3. 记录训练速度、显存占用、tokens/s
  4. 尝试修改 hidden_size 或 num_layers，观察显存变化

### 分布式训练

```python
def init_distributed_mode():
    # os.environ.get("RANK", -1)：尝试读取环境变量中的 RANK。RANK 是全局进程号（比如 8 卡训练时，RANK 是 0 到 7）。
    # 在用 torchrun 等工具启动分布式训练时，系统会自动注入 RANK 这个环境变量。如果是普通 python train.py 单卡启动，就没有这个变量，get 会返回默认值 -1。
    if int(os.environ.get("RANK", -1)) == -1:
        return 0  # 非DDP模式

    # init_process_group：初始化分布式进程组，相当于建群，让所有卡拉在一个群里。
    # 指定通信后端为 NCCL（NVIDIA Collective Communications Library）。这是 Nvidia 专为 GPU 通信打造的底层库，在多卡 GPU 训练中速度最快，是事实上的标准。
    dist.init_process_group(backend="nccl")
    # 获取当前进程的局部卡号：LOCAL_RANK 也是一个由启动器自动注入的环境变量，代表当前机器上的第几张卡。
    local_rank = int(os.environ["LOCAL_RANK"])

    torch.cuda.set_device(local_rank)  # 设置当前进程的 GPU 设备为 local_rank
    return local_rank  # 把当前的 local_rank 返回给主程序，后续的代码（比如把模型搬到卡上 model.to(device)、构建 DDP 等）都会用到这个变量。
```

```python
import os
import sys

__package__ = "trainer"
sys.path.append(os.path.abspath(os.path.join(os.path.dirname(__file__), '..'))) 

import argparse
import time
import warnings
import torch
import torch.distributed as dist
from contextlib import nullcontext
from torch import optim, nn
from torch.nn.parallel import DistributedDataParallel
from torch.utils.data import DataLoader, DistributedSampler
from model.model_minimind import MiniMindConfig
from dataset.lm_dataset import PretrainDataset
from trainer.trainer_utils import get_lr, Logger, is_main_process, lm_checkpoint, init_distributed_mode, setup_seed, \
    init_model, SkipBatchSampler

warnings.filterwarnings('ignore')


def train_epoch(epoch, loader, iters, start_step=0, wandb=None):
    start_time = time.time()
    last_step = start_step
    # 一个epoch里面的所有数据分割为batch size的数据，这个step就是多个batch
    for step, (input_ids, labels) in enumerate(loader, start=start_step + 1):  # 遍历每个Batch
        input_ids = input_ids.to(args.device)
        labels = labels.to(args.device)
        last_step = step
        lr = get_lr(epoch * iters + step, args.epochs * iters, args.learning_rate)  # 衰减的学习率，计算当前步的
        for param_group in optimizer.param_groups:  # 更新优化器学习率：将计算得到的新学习率手动赋值给优化器中的所有参数组。
            param_group['lr'] = lr

        with autocast_ctx:  # 开启混合精度上下文
            res = model(input_ids, labels=labels)
            loss = res.loss + res.aux_loss
            loss = loss / args.accumulation_steps  # 损失缩放（梯度累积）：将损失除以累积步数。这样在反向传播时，梯度也会相应缩小，从而在多次累积后求平均，等效于大Batch Size训练，且不爆显存。

        scaler.scale(loss).backward()  # 反向传播，使用 scaler 对损失进行缩放（防止FP16梯度下溢），然后执行反向传播计算梯度。

        if step % args.accumulation_steps == 0:  # 达到指定的step才进行参数更新
            scaler.unscale_(optimizer)  # 梯度反缩放：在更新权重前，将之前放大的梯度缩放回真实大小，以便进行梯度裁剪和参数更新。
            torch.nn.utils.clip_grad_norm_(model.parameters(), args.grad_clip)  # 梯度裁剪：防止梯度爆炸，将梯度的范数限制在指定范围内。

            scaler.step(optimizer)  # 更新参数：根据梯度和学习率，更新模型参数。
            scaler.update()  # 更新 scaler 状态：将梯度和学习率更新后的状态保存到 scaler 中，以便在下一次反向传播时使用。

            optimizer.zero_grad(set_to_none=True)  # 将优化器中的梯度清零。set_to_none=True 比直接置为0更高效，能减少内存占用和加速下一次计算。
        # 每隔 log_interval 步，或者在当前epoch的最后一步时，打印/记录日志。
        if step % args.log_interval == 0 or step == iters:
            spend_time = time.time() - start_time
            current_loss = loss.item() * args.accumulation_steps  # 真实的损失
            current_aux_loss = res.aux_loss.item() if res.aux_loss is not None else 0.0  # 获取辅助损失：如果存在辅助损失则提取其数值，否则记为0.0。
            current_logits_loss = current_loss - current_aux_loss  # 计算主损失
            current_lr = optimizer.param_groups[-1]['lr']  # 获取当前学习率：从优化器中提取最后一个参数组的学习率。
            eta_min = spend_time / max(step - start_step, 1) * (iters - step) // 60
            Logger(
                f'Epoch:[{epoch + 1}/{args.epochs}]({step}/{iters}), loss: {current_loss:.4f}, logits_loss: {current_logits_loss:.4f}, aux_loss: {current_aux_loss:.4f}, lr: {current_lr:.8f}, epoch_time: {eta_min:.1f}min')
            if wandb: wandb.log({"loss": current_loss, "logits_loss": current_logits_loss, "aux_loss": current_aux_loss,
                                 "learning_rate": current_lr, "epoch_time": eta_min})  # 记录到WandB
        # 每隔 save_interval 步或epoch末尾，且在分布式训练中的主进程上执行保存操作，避免多卡同时保存造成冲突。
        if (step % args.save_interval == 0 or step == iters) and is_main_process():
            model.eval()  # 保存前切换到eval模式，这会影响Dropout和BatchNorm等层的行为，确保保存的状态一致。
            moe_suffix = '_moe' if lm_config.use_moe else ''
            ckp = f'{args.save_dir}/{args.save_weight}_{lm_config.hidden_size}{moe_suffix}.pth'
            # 如果模型被 DistributedDataParallel (DDP) 包裹，需要通过 .module 访问真正的模型本体，否则直接使用原模型。
            raw_model = model.module if isinstance(model, DistributedDataParallel) else model
            # 如果模型使用了 torch.compile() 编译，模型会被包裹，通过 _orig_mod 属性可以拿到编译前的原始模型，以便正常获取 state_dict
            raw_model = getattr(raw_model, '_orig_mod', raw_model)
            state_dict = raw_model.state_dict()  # 提取模型参数
            # 保存模型参数，将所有参数转换为半精度（bfloat16），并将它们移动到CPU上。
            torch.save({k: v.half().cpu() for k, v in state_dict.items()}, ckp)
            # 调用自定义函数保存用于恢复训练的完整状态（包含模型、优化器、scaler、当前epoch和step等），以便下次断点续训。
            lm_checkpoint(lm_config, weight=args.save_weight, model=model, optimizer=optimizer, scaler=scaler,
                          epoch=epoch, step=step, wandb=wandb, save_dir='../checkpoints')
            model.train()
            del state_dict

        del input_ids, labels, res, loss

    #处理残余梯度
    if last_step > start_step and last_step % args.accumulation_steps != 0:
        scaler.unscale_(optimizer)
        torch.nn.utils.clip_grad_norm_(model.parameters(), args.grad_clip)
        scaler.step(optimizer)
        scaler.update()
        optimizer.zero_grad(set_to_none=True)


if __name__ == "__main__":
    parser = argparse.ArgumentParser(description="MiniMind Pretraining")
    parser.add_argument("--save_dir", type=str, default="../out", help="模型保存目录")
    parser.add_argument('--save_weight', default='pretrain', type=str, help="保存权重的前缀名")
    parser.add_argument("--epochs", type=int, default=2, help="训练轮数")
    parser.add_argument("--batch_size", type=int, default=32, help="batch size")
    parser.add_argument("--learning_rate", type=float, default=5e-4, help="初始学习率")
    parser.add_argument("--device", type=str, default="cuda:0" if torch.cuda.is_available() else "cpu", help="训练设备")
    parser.add_argument("--dtype", type=str, default="bfloat16", help="混合精度类型")
    parser.add_argument("--num_workers", type=int, default=8, help="数据加载线程数")
    parser.add_argument("--accumulation_steps", type=int, default=8, help="梯度累积步数")
    parser.add_argument("--grad_clip", type=float, default=1.0, help="梯度裁剪阈值")
    parser.add_argument("--log_interval", type=int, default=100, help="日志打印间隔")
    parser.add_argument("--save_interval", type=int, default=1000, help="模型保存间隔")
    parser.add_argument('--hidden_size', default=768, type=int, help="隐藏层维度")
    parser.add_argument('--num_hidden_layers', default=8, type=int, help="隐藏层数量")
    parser.add_argument('--max_seq_len', default=340, type=int, help="训练的最大截断长度（中文1token≈1.5~1.7字符）")
    parser.add_argument('--use_moe', default=0, type=int, choices=[0, 1], help="是否使用MoE架构（0=否，1=是）")
    parser.add_argument("--data_path", type=str, default="../dataset/pretrain_t2t_mini.jsonl", help="预训练数据路径")
    parser.add_argument('--from_weight', default='none', type=str, help="基于哪个权重训练，为none则从头开始")
    parser.add_argument('--from_resume', default=0, type=int, choices=[0, 1], help="是否自动检测&续训（0=否，1=是）")
    parser.add_argument("--use_wandb", action="store_true", help="是否使用wandb")
    parser.add_argument("--wandb_project", type=str, default="MiniMind-Pretrain", help="wandb项目名")
    parser.add_argument("--use_compile", default=0, type=int, choices=[0, 1],
                        help="是否使用torch.compile加速（0=否，1=是）")
    args = parser.parse_args()

    # ========== 1. 初始化环境和随机种子 ==========
    local_rank = init_distributed_mode()
    if dist.is_initialized(): args.device = f"cuda:{local_rank}"
    setup_seed(42 + (dist.get_rank() if dist.is_initialized() else 0))

    # ========== 2. 配置目录、模型参数、检查ckp ==========
    os.makedirs(args.save_dir, exist_ok=True)
    lm_config = MiniMindConfig(hidden_size=args.hidden_size, num_hidden_layers=args.num_hidden_layers,
                               use_moe=bool(args.use_moe))
    ckp_data = lm_checkpoint(lm_config, weight=args.save_weight,
                             save_dir='../checkpoints') if args.from_resume == 1 else None

    # ========== 3. 设置混合精度 ==========
    device_type = "cuda" if "cuda" in args.device else "cpu"
    dtype = torch.bfloat16 if args.dtype == "bfloat16" else torch.float16  # BFloat16，由谷歌提出，动态范围与FP32相同，不易溢出，适合训练大模型
    autocast_ctx = nullcontext() if device_type == "cpu" else torch.cuda.amp.autocast(dtype=dtype)  # 创建自动混合精度的上下文管理器：

    # ========== 4. 配wandb ==========
    # wandb Weights AND Biases
    wandb = None
    if args.use_wandb and is_main_process():  # 使用wandb and 当前进程是主进程
        import swanlab as wandb  # 与wandb兼容的国产工具

        # 如果是从断点恢复训练（ckp_data 不为空），就尝试从中取出之前保存的 wandb_id。每个 wandb 运行实例都有唯一的 ID。
        # 如果是全新训练（ckp_data 为空），则 wandb_id 为 None。
        wandb_id = ckp_data.get('wandb_id') if ckp_data else None

        # 如果上一步获取到了 wandb_id（说明是断点续训），则将 resume 设为 'must'。
        # 这告诉实验记录工具：必须找到这个 ID 对应的历史记录并接续写入，如果找不到就报错。

        resume = 'must' if wandb_id else None
        wandb_run_name = f"MiniMind-Pretrain-Epoch-{args.epochs}-BatchSize-{args.batch_size}-LearningRate-{args.learning_rate}"

        # project：项目名称。在后台会将相似的实验归到同一个 Project 下管理（例如叫 “MiniMind-Pretrain”）。
        # name：当前这次运行（Run）的名称，即上一步拼接的长字符串。
        # id：当前运行的唯一标识。如果是新训练，不传或传 None 会自动生成；如果是续训，传之前保存的 ID。
        # resume：续训策略，决定了当 ID 已存在时，是新建一条还是接着旧数据画图。
        wandb.init(project=args.wandb_project, name=wandb_run_name, id=wandb_id, resume=resume)

    # ========== 5. 定义模型、数据、优化器 ==========
    model, tokenizer = init_model(lm_config, args.from_weight, device=args.device)
    train_ds = PretrainDataset(args.data_path, tokenizer, max_length=args.max_seq_len)
    train_sampler = DistributedSampler(train_ds) if dist.is_initialized() else None
    scaler = torch.cuda.amp.GradScaler(enabled=(args.dtype == 'float16'))
    optimizer = optim.AdamW(model.parameters(), lr=args.learning_rate)

    # ========== 6. 从ckp恢复状态 ==========
    start_epoch, start_step = 0, 0
    if ckp_data:  # 如果有检查点数据，就恢复模型、优化器、梯度缩放器的状态。
        model.load_state_dict(ckp_data['model'])
        optimizer.load_state_dict(ckp_data['optimizer'])
        scaler.load_state_dict(ckp_data['scaler'])
        start_epoch = ckp_data['epoch']
        start_step = ckp_data.get('step', 0)

    # ========== 7. 编译和分布式包装 ==========
    if args.use_compile == 1:
        # 编译模型，加速训练,torch2的特性，将动态图转换为静态图，提高训练效率，减少GPU显存读写次数和python开销，
        # 通常带来10%-30%的加速
        # 返回一个经过包装的编译后对象
        model = torch.compile(model)
        Logger('torch.compile enabled')
    if dist.is_initialized():
        model = DistributedDataParallel(model, device_ids=[local_rank])  # 分布式包装模型，将模型分配到多个GPU上并行训练

    # ========== 8. 开始训练 ==========
    for epoch in range(start_epoch, args.epochs):  # 继续训练
        # 分布式训练的必须操作：这是一个简写的 if 语句，等价于 if train_sampler: train_sampler.set_epoch(epoch)。
        # set_epoch(epoch) 可以让数据采样器根据当前的 epoch 打乱种子，确保每个 epoch 各卡拿到的数据分布不同。这是 DDP 训练中极其容易漏掉导致模型不收敛的 Bu
        train_sampler and train_sampler.set_epoch(epoch)
        setup_seed(42 + epoch);  # 虽然单个epoch是随机的，但总体是有规律的、可复现的。

        # 生成一个长度为数据集大小的随机打乱索引列表，用于随机抽取数据。
        # randperm，输出一个长度为 n 的 1D 张量（Tensor），包含从 0 到 n-1 的整数，且这些整数是随机打乱顺序的。
        indices = torch.randperm(len(train_ds)).tolist()
        skip = start_step if (epoch == start_epoch and start_step > 0) else 0  # 计算当前epoch的需要跳过的步数

        # 构建跳步采样器：
        # 这也是一个非常巧妙的工程实现。普通的 DataLoader 无法直接从第 N 个 batch 开始加载，如果强行从第 N 个 batch 开始，由于数据打乱机制，后续拿到的数据顺序可能和上次中断前不一致。
        # 这里自定义了一个 SkipBatchSampler，它会在内部直接跳过前 skip 个 batch 的索引，确保续训后拿到的第一个 batch，正好就是上次中断前的下一个 batch，且数据顺序完全吻合。
        # train_sampler or indices：如果是分布式训练用 train_sampler，单机训练用刚生成的 indices
        batch_sampler = SkipBatchSampler(train_sampler or indices, args.batch_size, skip)  # 多卡逻辑或单卡逻辑

        # pin_memory 锁页内存，将数据先分配在 CPU 的锁页内存中，能加快数据从 CPU 转移到 GPU 显存的速度。
        loader = DataLoader(train_ds, batch_sampler=batch_sampler, num_workers=args.num_workers, pin_memory=True)
        if skip > 0:
            Logger(f'Epoch [{epoch + 1}/{args.epochs}]: 跳过前{start_step}个step，从step {start_step + 1}开始')
            train_epoch(epoch, loader, len(loader) + skip, start_step, wandb)
        else:
            train_epoch(epoch, loader, len(loader), 0, wandb)

    # ========== 9. 清理分布进程 ==========
    if dist.is_initialized(): dist.destroy_process_group()  # 销毁进程组

```

包装好了的sampler，这里只跳过多少个batch，跳过的step由train_epoch中处理

```python
class SkipBatchSampler(Sampler):
    def __init__(self, sampler, batch_size, skip_batches=0):
        self.sampler = sampler
        self.batch_size = batch_size
        self.skip_batches = skip_batches

    def __iter__(self):
        batch = []
        skipped = 0
        for idx in self.sampler:
            batch.append(idx)
            if len(batch) == self.batch_size:# 够一个batch了
                if skipped < self.skip_batches:
                    skipped += 1 #跳过了多少个batches
                    batch = [] #清空batch
                    continue # 直接进入下一次循环，不执行后面的 yield
                yield batch # 如果不需要跳过了，就把这个装满的篮子交出去
                batch = [] #清空准备装下一个Batch
        # 循环结束，处理最后剩下的不够一个 batch_size 的“尾数”
        if len(batch) > 0 and skipped >= self.skip_batches:
            yield batch # 如果最后剩下的不够一个 batch_size 的“尾数”，也交出去

    def __len__(self):
        total_batches = (len(self.sampler) + self.batch_size - 1) // self.batch_size
        return max(0, total_batches - self.skip_batches)
```

这里的逻辑还是蛮复杂的，包括了多卡训练，训练中继，跳过batch（因为是按随机种子分割的，可以复现）、跳过step，

核心的切入点就是

```
batch_sampler = SkipBatchSampler(train_sampler or indices, args.batch_size, skip)  # 多卡逻辑或单卡逻辑

和

def init_distributed_mode():
```



我这里就没有在本机上做预训练实验了，已经在服务器上挂着跑呢。



---

## Day 3：SFT 数据准备和模板理解

* **任务**：理解 SFT 数据与 Pretrain 的区别

🎯 目标

- 熟悉 SFT 数据格式和 instruction 模板
- 理解 user/assistant/system token 拼接方式
- 确认 loss mask 的作用

🧰 操作步骤

1. 查看 `train/train_full_sft.py`和`dataset/lm_dataset.py`，理解 `SFTDataset` 构造方法。

```python

class SFTDataset(Dataset):
    def __init__(self, jsonl_path, tokenizer, max_length=1024):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        # 定义数据集的 schema（模式）：
        # 顶层字段 conversations 是一个列表
        # 列表中每个元素是一个字典，包含 5 个字符串字段
        # reasoning_content：思维链/推理内容（用于支持类似 DeepSeek-R1 的推理模型）
        # 使用 Features 显式声明类型，确保加载时类型安全，避免自动推断出错
        features = Features({'conversations': [
            {'role': Value('string'), 'content': Value('string'), 'reasoning_content': Value('string'),
             'tools': Value('string'), 'tool_calls': Value('string')}]})

        # 按照上面定义的 schema 解析，所有字段都作为字符串读取
        self.samples = load_dataset('json', data_files=jsonl_path, split='train', features=features)

        # bos_id：将 <bos_token>assistant\n（如 <｜begin▁of▁sentence｜>assistant\n）编码为 token ID 列表。这个模式标志着 assistant 回复的开始位置。
        self.bos_id = tokenizer(f'{tokenizer.bos_token}assistant\n', add_special_tokens=False).input_ids
        self.eos_id = tokenizer(f'{tokenizer.eos_token}\n', add_special_tokens=False).input_ids

    def __len__(self):
        return len(self.samples)

    def create_chat_prompt(self, conversations):
        messages = []
        tools = None
        for message in conversations:
            # 遍历message
            message = dict(message)
            # 将解析后的工具定义存到 tools 变量，后续传给 apply_chat_template
            if message.get("role") == "system" and message.get("tools"):
                tools = json.loads(message["tools"]) if isinstance(message["tools"], str) else message["tools"]
            # 将处理后的消息加入列表。
            if message.get("tool_calls") and isinstance(message["tool_calls"], str):
                message["tool_calls"] = json.loads(message["tool_calls"])  # 将工具调用的字符串转为json对象
            messages.append(message)
        # 调用分词器的聊天模板，将多轮对话格式化为一个完整的字符串。
        return self.tokenizer.apply_chat_template(
            messages,
            tokenize=False,  # 返回字符串而非 token IDs
            add_generation_prompt=False,  # 不在末尾追加 assistant 的生成提示
            tools=tools  # 传入工具定义，模板会自动在 system 消息后插入工具 schema
        )

    def generate_labels(self, input_ids):
        labels = [-100] * len(input_ids)  # 所有位置都设置为哦-100
        i = 0
        while i < len(input_ids):
            if input_ids[i:i + len(self.bos_id)] == self.bos_id:  # 找到assistant回复的开始位置
                start = i + len(self.bos_id)
                end = start
                while end < len(input_ids):
                    if input_ids[end:end + len(self.eos_id)] == self.eos_id:  # 找到结束的位置
                        break
                    end += 1
                # 将助手回复区间内的 label 从 -100 替换为真实的 token ID。
                for j in range(start, min(end + len(self.eos_id), self.max_length)):
                    labels[j] = input_ids[j]
                # 跳过当前助手回复区间，继续查找下一个开始位置。
                i = end + len(self.eos_id) if end < len(input_ids) else len(input_ids)
            else:
                i += 1
        return labels

    def __getitem__(self, index):
        sample = self.samples[index]
        conversations = pre_processing_chat(sample['conversations'])  # 如果没有sys的提示词就随机注入一个预定义的提示词
        prompt = self.create_chat_prompt(conversations)  # 就是把tools抓出来
        # 后处理chat，移除空思考标签?不太理解为什么要这么做，也许是协议问题？可能数据清洗的时候有问题吧
        # 数据构造者把 <think/> 直接写死塞进了 content 字符串的开头，而不是利用独立的数据结构让 apply_chat_template 去自动渲染。
        prompt = post_processing_chat(prompt)
        input_ids = self.tokenizer(prompt).input_ids[:self.max_length]  # 输入截断
        input_ids += [self.tokenizer.pad_token_id] * (self.max_length - len(input_ids))  # pad 尾步字符
        labels = self.generate_labels(input_ids)  # 掩码labels
        # # === 调试打印 ===
        # print(f"\n--- Sample {index} ---")
        # for i, (x, y) in enumerate(zip(input_ids[:-1], labels[1:])):
        #     print(f"{i:3d}: X={self.tokenizer.decode([x])!r:16s} ---> Y={self.tokenizer.decode([input_ids[i+1]])!r:16s} label={y}")
        # # ================
        return torch.tensor(input_ids, dtype=torch.long), torch.tensor(labels, dtype=torch.long)
```



```json
{
    "conversations": [
        {
            "role": "system",
            "content": "",
            "tools": "[{\"function\":{\"name\":\"get_exchange_rate\", \"description\":\"查询两种货币之间的实时汇率\", \"parameters\":{\"type\":\"object\", \"properties\":{\"from_currency\":{\"type\":\"string\", \"description\":\"源货币代码，如USD、CNY、EUR\"}, \"to_currency\":{\"type\":\"string\", \"description\":\"目标货币代码，如USD、CNY、EUR\"}}, \"required\":[\"from_currency\", \"to_currency\"]}}}, {\"function\":{\"name\":\"random_number\", \"description\":\"生成指定范围内的随机数\", \"parameters\":{\"type\":\"object\", \"properties\":{\"min\":{\"type\":\"integer\", \"description\":\"最小值\", \"default\":0}, \"max\":{\"type\":\"integer\", \"description\":\"最大值\", \"default\":100}}, \"required\":[]}}}]"
        },
        {
            "role": "user",
            "content": "请随机生成一个1到100之间的数字，并查询美元对人民币的当前汇率"
        },
        {
            "role": "assistant",
            "content": "生成1到100之间的随机数并查询当前美元对人民币汇率",
            "tool_calls": "[{\"function\":{\"name\":\"random_number\", \"arguments\":\"{\\\"min\\\":1, \\\"max\\\":100}\"}}, {\"function\":{\"name\":\"get_exchange_rate\", \"arguments\":\"{\\\"from_currency\\\":\\\"USD\\\", \\\"to_currency\\\":\\\"CNY\\\"}\"}}]"
        },
        {
            "role": "tool",
            "content": "{\"result\":69}\n{\"from_currency\":\"USD\", \"to_currency\":\"CNY\", \"rate\":7.21, \"timestamp\":\"2024-04-05T10:30:00Z\"}"
        },
        {
            "role": "assistant",
            "content": "随机生成的数字是：69。  \n美元对人民币的当前汇率是：1美元 = 7.21人民币。"
        }
    ]
}
```

---

## Day 4：SFT 小实验训练

* **任务**：用小数据跑 SFT，理解 loss mask
* **操作**：
  1. 训练 1–2k step
  2. 比较 pretrain 模型与 SFT 模型的输出差异
  3. 记录 loss、输出格式、胡说程度
  4. 尝试修改模板，看模型输出变化

就不做了，有空读者可以去尝试下，会先读取预训练的`'../out/pretrain_32.pth`然后再训练。

---

## Day 5：LoRA 原理与实验

**任务**：理解 LoRA 如何微调权重

- 理解 LoRA 低秩矩阵 A/B 的作用
- 比较显存、训练速度和参数量

**操作**：

1. 阅读 `train/lora_train.py`
2. 确认低秩矩阵 A/B 的形状和合并方式
3. 小规模 LoRA 微调实验：对比SFT显存占用和训练速度，以及参数量计算
4. 输出参数量统计表：全参 SFT vs LoRA



普通全参数微调会更新模型中的大量线性层权重：

```
W_new = W + ΔW
```

LoRA 不直接训练完整的 `ΔW`，而是把它近似为两个小矩阵乘积：
$$
\Delta W = BA
$$
其中：

```
W：原始大模型参数，冻结
A、B：LoRA 新增的小矩阵，需要训练
```

前向传播时相当于：

```
output = x @ W + x @ A @ B
```

```python
import torch
from torch import optim, nn


# 定义Lora网络结构
class LoRA(nn.Module):
    def __init__(self, in_features, out_features, rank):
        super().__init__()
        self.rank = rank  # LoRA的秩（rank），控制低秩矩阵的大小
        self.A = nn.Linear(in_features, rank, bias=False)  # 低秩矩阵A
        self.B = nn.Linear(rank, out_features, bias=False)  # 低秩矩阵B
        # 矩阵A高斯初始化
        self.A.weight.data.normal_(mean=0.0, std=0.02)
        # 矩阵B全0初始化
        self.B.weight.data.zero_()

    def forward(self, x):
        return self.B(self.A(x))


def apply_lora(model, rank=16):
    for name, module in model.named_modules():
        if isinstance(module, nn.Linear) and module.weight.shape[0] == module.weight.shape[1]:
            lora = LoRA(module.weight.shape[0], module.weight.shape[1], rank=rank).to(model.device)
            setattr(module, "lora", lora)
            original_forward = module.forward

            # 显式绑定
            def forward_with_lora(x, layer1=original_forward, layer2=lora):
                return layer1(x) + layer2(x)

            module.forward = forward_with_lora


def load_lora(model, path):
    state_dict = torch.load(path, map_location=model.device)
    state_dict = {(k[7:] if k.startswith('module.') else k): v for k, v in state_dict.items()}

    for name, module in model.named_modules():
        if hasattr(module, 'lora'):
            lora_state = {k.replace(f'{name}.lora.', ''): v for k, v in state_dict.items() if f'{name}.lora.' in k}
            module.lora.load_state_dict(lora_state)


def save_lora(model, path):
    raw_model = getattr(model, '_orig_mod', model)
    state_dict = {}
    for name, module in raw_model.named_modules():
        if hasattr(module, 'lora'):
            clean_name = name[7:] if name.startswith("module.") else name
            lora_state = {f'{clean_name}.lora.{k}': v.cpu().half() for k, v in module.lora.state_dict().items()}
            state_dict.update(lora_state)
    torch.save(state_dict, path)


def merge_lora(model, lora_path, save_path):
    # 微调后的模型与原始基础模型进行权重合并，并将合并后的完整模型保存到本地。
    load_lora(model, lora_path)
    raw_model = getattr(model, '_orig_mod', model)
    state_dict = {k: v.cpu().half() for k, v in raw_model.state_dict().items() if '.lora.' not in k}
    for name, module in raw_model.named_modules():
        if isinstance(module, nn.Linear) and '.lora.' not in name:
            state_dict[f'{name}.weight'] = module.weight.data.clone().cpu().half()
            if hasattr(module, 'lora'):
                state_dict[f'{name}.weight'] += (module.lora.B.weight.data @ module.lora.A.weight.data).cpu().half() #微调后的权重加上原来的权重
    torch.save(state_dict, save_path)

```

理解还是挺容易的，我这里就不做赘述了，代码蛮简单的，重点再merge_lora融合上

核心不同的地方

```python
# ========== 5. 定义模型、应用LoRA、冻结非LoRA参数 ==========
model, tokenizer = init_model(lm_config, args.from_weight, device=args.device)
apply_lora(model)

# 统计参数
total_params = sum(p.numel() for p in model.parameters())
lora_params_count = sum(p.numel() for name, p in model.named_parameters() if 'lora' in name)
Logger(f"LLM 总参数量: {total_params / 1e6:.3f} M")
Logger(f"LoRA 参数量: {lora_params_count / 1e6:.3f} M")
Logger(f"LoRA 参数占比: {lora_params_count / total_params * 100:.2f}%")

# 冻结非LoRA参数，收集LoRA参数
lora_params = []
for name, param in model.named_parameters():
    if 'lora' in name:
        param.requires_grad = True
        lora_params.append(param)
    else:
        param.requires_grad = False #冻结参数
```



注意：模型只是将lora参数保存下来了，后面还需要将lora参数融合到原始参数上

```python
def convert_merge_base_lora(base_torch_path, lora_path, merged_torch_path):
    device = torch.device('cuda' if torch.cuda.is_available() else 'cpu')
    lm_model = MiniMindForCausalLM(lm_config).to(device)
    state_dict = torch.load(base_torch_path, map_location=device)
    lm_model.load_state_dict(state_dict, strict=False)
    apply_lora(lm_model)
    merge_lora(lm_model, lora_path, merged_torch_path)
    print(f"LoRA 已合并并保存为基模结构 PyTorch 格式: {merged_torch_path}")
    
    

    # # merge lora
    # base_torch_path = f"../out/full_sft_{lm_config.hidden_size}{'_moe' if lm_config.use_moe else ''}.pth"
    # lora_path = f"../out/lora_identity_{lm_config.hidden_size}{'_moe' if lm_config.use_moe else ''}.pth"
    # merged_torch_path = f"../out/merge_identity_{lm_config.hidden_size}{'_moe' if lm_config.use_moe else ''}.pth"
    # convert_merge_base_lora(base_torch_path, lora_path, merged_torch_path)

```

实验细节就不做了，大致理论和流程理解了就行

```
W_new = W + ΔW
```



---

## Day 6：DPO 数据与训练

* **任务**：理解 DPO 偏好训练
* **操作**：
  1. 查看 `train/dpo_train.py`
  2. 准备 `prompt + chosen + rejected` 数据
  3. 运行 DPO 小实验，观察 loss 变化
  4. 对比 SFT/DPO 输出，理解概率优化作用

day5完成的是

```
问题 → 标准答案
```

今天需要完成

```
同一个问题 → chosen：更好的回答
同一个问题 → rejected：较差的回答
```

训练目标是

```
让策略模型更偏向 chosen 回答，
同时降低 rejected 回答的相对概率。
```

整个流程如下：

```text
                ┌──────────────────┐
chosen 输入 ───▶│   Policy Model   │──▶ chosen 的概率
                └──────────────────┘
                         │
                         │      与 Reference Model 对比
                         ▼
                     DPO Loss
                         ▲
                         │
                ┌──────────────────┐
rejected 输入 ─▶│   Policy Model   │──▶ rejected 的概率
                └──────────────────┘
```

脚本中存在两个模型：

| 模型     | 变量名      | 作用                                  | 是否训练 |
| -------- | ----------- | ------------------------------------- | -------- |
| 策略模型 | `model`     | 最终要优化的 MiniMind 模型            | 是       |
| 参考模型 | `ref_model` | 约束策略模型不要偏离原始 SFT 模型太远 | 否       |

这份代码的核心思想可以概括为：

```text
在参考模型看来，chosen 比 rejected 好多少；
在当前策略模型看来，chosen 比 rejected 好多少；

如果策略模型比参考模型更加偏好 chosen，
说明训练方向正确。
```

附件代码整体就是围绕这一目标构建的

### DPO 与 SFT的区别

```
prompt + chosen
prompt + rejected
       ↓
直接比较两者概率
       ↓
更新策略模型
```

DPO其实上是PPO的一种简化。

代码改成：

```python
x_chosen = batch['x_chosen'].to(args.device)
x_rejected = batch['x_rejected'].to(args.device)

y_chosen = batch['y_chosen'].to(args.device)
y_rejected = batch['y_rejected'].to(args.device)

mask_chosen = batch['mask_chosen'].to(args.device)
mask_rejected = batch['mask_rejected'].to(args.device)
```

一条数据包含两种回答：

```
chosen   ：更好的回答
rejected ：更差的回答
```

例如：

```
问题：解释什么是 Transformer

chosen：
Transformer 是一种基于自注意力机制的神经网络结构……

rejected：
Transformer 就是一个普通循环网络。
```

DPO 不要求模型机械复现某一个答案，而是学习：

```
更偏好 chosen，降低 rejected 的相对概率。
```





一个数据样本

```json
{
    "chosen": [
        {
            "content": "continue",
            "role": "user"
        },
        {
            "content": "As Recharge Retreats grows, we plan to expand our team with additional event coordinators and marketing specialists. To accommodate this growth, we will establish an office space that fosters collaboration and creativity among team members. We will also prioritize remote work options and streamlined communication tools to support remote team members.\n\nIn addition, we aim to diversify our retreat offerings to cater to different themes, such as solo travel retreats, wellness retreats for parents, and spiritual journeys. We will seek to partner with multiple venues across various locations worldwide to provide a diverse range of retreat experiences.\n\nWe will also expand our marketing efforts to reach a wider audience, maximizing brand exposure through influencer partnerships, experiential marketing, and virtual events. We will continuously strive to refine our services based on client feedback and identify industry trends to adapt our offerings to the ever-changing needs and preferences of our target market.\n\nAs we scale, we will remain committed to upholding our mission to empower and connect women through transformative retreat experiences that prioritize personal growth, wellness, and connection.",
            "role": "assistant"
        }
    ],
    "rejected": [
        {
            "content": "continue",
            "role": "user"
        },
        {
            "content": "A. Scaling:\n\n1. Offer a franchise model: Allow others to open and operate Recharge Retreats franchises in different regions, expanding our reach and revenue potential.\n2. Develop online courses and coaching programs: Create supplementary income streams by offering training and support to individuals and organizations interested in organizing their own retreats.\n\nB. Diversification:\n\n1. Expand service offerings: Consider adding additional retreat-related services, such as coaching, ongoing support, retreat evaluation, or retreat financing options.\n2. Target new markets: Broadening our target market to include other demographics, such as men, families, or specific interest groups.\n\nC. International Expansion:\n\n1. Partner with local organizations: Collaborate with women's organizations abroad to plan international retreats and expand our global presence.\n2. Seek funding opportunities: Pursue grants, investments, and loans to finance international retreats and support expansion in new markets.\n\nBy following this business plan, Recharge Retreats has the potential to become a leading provider of women's retreat services, empowering individuals and organizations to foster growth, connection, and personal transformation through memorable and life-enhancing experiences.",
            "role": "assistant"
        }
    ]
}
```



ok接下来就是查看数据集了

```python
class DPODataset(Dataset):
    def __init__(self, file_path, tokenizer, max_length=4096):
        super().__init__()
        self.tokenizer = tokenizer
        self.max_length = max_length
        self.padding = tokenizer.pad_token_id if tokenizer.pad_token_id is not None else 0
        self.bos_id = tokenizer(f'{tokenizer.bos_token}assistant\n', add_special_tokens=False).input_ids
        self.eos_id = tokenizer(f'{tokenizer.eos_token}\n', add_special_tokens=False).input_ids
        self.samples = load_dataset('json', data_files=file_path, split='train')

    def __len__(self):
        return len(self.samples)

    def __getitem__(self, index):
        # 总体思路和之前一样，不过这里弄了2个prompt，一个是chosen，一个是rejected
        sample = self.samples[index]
        chosen = sample['chosen']  # 是一个 list，里面包含若干 {role, content}
        rejected = sample['rejected']  # 同上
        chosen_prompt = self.tokenizer.apply_chat_template(
            chosen, tokenize=False, add_generation_prompt=False
        )
        chosen_prompt = post_processing_chat(chosen_prompt)

        rejected_prompt = self.tokenizer.apply_chat_template(
            rejected, tokenize=False, add_generation_prompt=False
        )
        rejected_prompt = post_processing_chat(rejected_prompt)
        chosen_encoding = self.tokenizer(
            chosen_prompt, truncation=True, max_length=self.max_length, padding='max_length'
        )
        rejected_encoding = self.tokenizer(
            rejected_prompt, truncation=True, max_length=self.max_length, padding='max_length'
        )

        chosen_input_ids = chosen_encoding['input_ids']
        chosen_loss_mask = self.generate_loss_mask(chosen_input_ids)  # 掩码需要的窗口

        rejected_input_ids = rejected_encoding['input_ids']
        rejected_loss_mask = self.generate_loss_mask(rejected_input_ids)  # 掩码需要的窗口
        x_chosen = torch.tensor(chosen_input_ids[:-1], dtype=torch.long)  # 输入，去掉最后一个token
        y_chosen = torch.tensor(chosen_input_ids[1:], dtype=torch.long)  # 输出，去掉第一个token
        mask_chosen = torch.tensor(chosen_loss_mask[1:], dtype=torch.long)  # 掩码，去掉第一个token
        x_rejected = torch.tensor(rejected_input_ids[:-1], dtype=torch.long)  # 输入，去掉最后一个token
        y_rejected = torch.tensor(rejected_input_ids[1:], dtype=torch.long)  # 输出，去掉第一个token
        mask_rejected = torch.tensor(rejected_loss_mask[1:], dtype=torch.long)  # 掩码，去掉第一个token

        return {
            'x_chosen': x_chosen,
            'y_chosen': y_chosen,
            'mask_chosen': mask_chosen,
            'x_rejected': x_rejected,
            'y_rejected': y_rejected,
            'mask_rejected': mask_rejected
        }

    def generate_loss_mask(self, input_ids):
        loss_mask = [0] * len(input_ids)  # 初始化全0的掩码    # 0 表示该位置不计算损失，1 表示计算损失
        i = 0
        while i < len(input_ids):
            if input_ids[i:i + len(self.bos_id)] == self.bos_id:
                start = i + len(self.bos_id)
                end = start
                while end < len(input_ids):
                    if input_ids[end:end + len(self.eos_id)] == self.eos_id:
                        break
                    end += 1
                # 找到需要训练的窗口，将窗口内的位置设为1，其他位置设为0
                for j in range(start, min(end + len(self.eos_id), self.max_length)):
                    loss_mask[j] = 1
                # 结尾符过完
                i = end + len(self.eos_id) if end < len(input_ids) else len(input_ids)
            else:
                i += 1
        return loss_mask
```

数据集没有特别大的变化，以前是返回一个，现在返回两个

核心变动
以前

```
model(input)
  ↓
和标准答案计算交叉熵
  ↓
更新 model
```

DPO 中

```python
# ========== 5. 定义模型和参考模型 ==========
model, tokenizer = init_model(lm_config, args.from_weight, device=args.device)
Logger(f'策略模型总参数量：{sum(p.numel() for p in model.parameters()) / 1e6:.3f} M')
# 初始化参考模型（ref_model冻结）
ref_model, _ = init_model(lm_config, args.from_weight, device=args.device)
ref_model.eval()
ref_model.requires_grad_(False)
Logger(f'参考模型总参数量：{sum(p.numel() for p in ref_model.parameters()) / 1e6:.3f} M')

train_ds = DPODataset(args.data_path, tokenizer, max_length=args.max_seq_len)
train_sampler = DistributedSampler(train_ds) if dist.is_initialized() else None
scaler = torch.cuda.amp.GradScaler(enabled=(args.dtype == 'float16'))
optimizer = optim.AdamW(model.parameters(), lr=args.learning_rate)

```

接下来检查训练的方式

```python
def train_epoch(epoch, loader, iters, ref_model, lm_config, start_step=0, wandb=None, beta=0.1):
    start_time = time.time()
    last_step = start_step

    for step, batch in enumerate(loader, start=start_step + 1):
        last_step = step
        x_chosen = batch['x_chosen'].to(args.device)
        x_rejected = batch['x_rejected'].to(args.device)
        y_chosen = batch['y_chosen'].to(args.device)
        y_rejected = batch['y_rejected'].to(args.device)
        mask_chosen = batch['mask_chosen'].to(args.device)
        mask_rejected = batch['mask_rejected'].to(args.device)
        x = torch.cat([x_chosen, x_rejected], dim=0)  # 拼接chosen和rejected的输入
        y = torch.cat([y_chosen, y_rejected], dim=0)  # 拼接chosen和rejected的输出
        mask = torch.cat([mask_chosen, mask_rejected], dim=0)  # 拼接chosen和rejected的掩码

        lr = get_lr(epoch * iters + step, args.epochs * iters, args.learning_rate)
        for param_group in optimizer.param_groups:
            param_group['lr'] = lr

        with autocast_ctx:  # 混合精度
            with torch.no_grad():
                ref_outputs = ref_model(x)
                ref_logits = ref_outputs.logits
            ref_log_probs = logits_to_log_probs(ref_logits, y)  # 参考模型的log概率

            outputs = model(x)
            logits = outputs.logits
            policy_log_probs = logits_to_log_probs(logits, y)  # 策略模型的log概率

            dpo_loss_val = dpo_loss(ref_log_probs, policy_log_probs, mask, beta=beta)  # DPO损失
            loss = dpo_loss_val + outputs.aux_loss  # 总损失
            loss = loss / args.accumulation_steps

        scaler.scale(loss).backward()

        if step % args.accumulation_steps == 0:  # 更新参数
            scaler.unscale_(optimizer)
            torch.nn.utils.clip_grad_norm_(model.parameters(), args.grad_clip)
            scaler.step(optimizer)
            scaler.update()
            optimizer.zero_grad(set_to_none=True)

        if step % args.log_interval == 0 or step == iters:  # 打印日志
            spend_time = time.time() - start_time
            current_loss = loss.item() * args.accumulation_steps
            current_dpo_loss = dpo_loss_val.item()
            current_aux_loss = outputs.aux_loss.item()
            current_lr = optimizer.param_groups[-1]['lr']
            eta_min = spend_time / max(step - start_step, 1) * (iters - step) // 60

            Logger(
                f'Epoch:[{epoch + 1}/{args.epochs}]({step}/{iters}), loss: {current_loss:.4f}, dpo_loss: {current_dpo_loss:.4f}, aux_loss: {current_aux_loss:.4f}, learning_rate: {current_lr:.8f}, epoch_time: {eta_min:.3f}min')

            if wandb: wandb.log({"loss": current_loss, "dpo_loss": current_dpo_loss, "aux_loss": current_aux_loss,
                                 "learning_rate": current_lr, "epoch_time": eta_min})

        if (step % args.save_interval == 0 or step == iters) and is_main_process():  # 保存模型
            model.eval()
            moe_suffix = '_moe' if lm_config.use_moe else ''
            ckp = f'{args.save_dir}/{args.save_weight}_{lm_config.hidden_size}{moe_suffix}.pth'
            raw_model = model.module if isinstance(model, DistributedDataParallel) else model
            raw_model = getattr(raw_model, '_orig_mod', raw_model)
            state_dict = raw_model.state_dict()
            torch.save({k: v.half().cpu() for k, v in state_dict.items()}, ckp)
            lm_checkpoint(lm_config, weight=args.save_weight, model=model, optimizer=optimizer, scaler=scaler,
                          epoch=epoch, step=step, wandb=wandb, save_dir='../checkpoints')
            model.train()
            del state_dict

        del x_chosen, x_rejected, y_chosen, y_rejected, mask_chosen, mask_rejected, x, y, mask
        del ref_outputs, ref_logits, ref_log_probs, outputs, logits, policy_log_probs, loss

    # 最后一个step，更新参数
    if last_step > start_step and last_step % args.accumulation_steps != 0:
        scaler.unscale_(optimizer)
        torch.nn.utils.clip_grad_norm_(model.parameters(), args.grad_clip)
        scaler.step(optimizer)
        scaler.update()
        optimizer.zero_grad(set_to_none=True)
```

SFT 中

核心损失通常类似：

```python
loss = F.cross_entropy(
    logits.view(-1, vocab_size),
    labels.view(-1),
    reduction='none'
)
loss = (loss * loss_mask).sum() / loss_mask.sum()
```

含义是：

```
标准答案 token 预测得越准，loss 越小。
```

SFT 关注的是：

```
模型是否能生成正确答案。
```



DPO 中

当前代码不直接使用普通交叉熵，而是先取出每个真实 token 的概率：

```python
ref_log_probs = logits_to_log_probs(ref_logits, y)
policy_log_probs = logits_to_log_probs(logits, y)
```

然后计算：

```python
dpo_loss_val = dpo_loss(
    ref_log_probs,
    policy_log_probs,
    mask,
    beta=beta
)
```

DPO 关注的是：

```
在当前模型中，chosen 相对 rejected 的优势，
是否比原始 SFT 模型中的优势更大。
```

具体做法在dpo_loss中

```python
def logits_to_log_probs(logits, labels):
    # logits shape: (batch_size, seq_len, vocab_size)
    # labels shape: (batch_size, seq_len)
    # log_probs shape: (batch_size, seq_len)
    log_probs = F.log_softmax(logits, dim=2)  # 计算对数softmax概率
    #gather 沿着词表维度（dim=2），根据 labels 提取出对应位置的值。此时形状为 (B, L, 1)。
    #假设句子是 “I love AI”，当输入 “I love” 时，模型预测下一个词，labels 这里是 “AI”。这个函数就是去查模型预测词表中 “AI” 这个词的对数概率是多少。
    log_probs_per_token = torch.gather(log_probs, dim=2, index=labels.unsqueeze(2)).squeeze(-1)  # 提取每个目标 Token 的对数概率
    return log_probs_per_token


def dpo_loss(ref_log_probs, policy_log_probs, mask, beta):
    #这个函数计算了 DPO 算法的损失值，目的是让模型在“好回答”上的概率变高，在“坏回答”上的概率变低，同时保持与参考模型（通常是初始的 SFT 模型）的相对距离。
    # ref_log_probs 和 policy_log_probs 都是 shape: (batch_size, seq_len)

    # 对每个样本，计算参考模型和策略模型的对数概率
    ref_log_probs = (ref_log_probs * mask).sum(dim=1)
    policy_log_probs = (policy_log_probs * mask).sum(dim=1)

    # 将 chosen 和 rejected 数据分开
    batch_size = ref_log_probs.shape[0]
    chosen_ref_log_probs = ref_log_probs[:batch_size // 2]
    reject_ref_log_probs = ref_log_probs[batch_size // 2:]
    chosen_policy_log_probs = policy_log_probs[:batch_size // 2]
    reject_policy_log_probs = policy_log_probs[batch_size // 2:]

    # 计算 Logratios (对数比率)，使得偏好 chosen 而拒绝 rejected
    pi_logratios = chosen_policy_log_probs - reject_policy_log_probs
    ref_logratios = chosen_ref_log_probs - reject_ref_log_probs
    logits = pi_logratios - ref_logratios
    loss = -F.logsigmoid(beta * logits)
    return loss.mean()
```



这里最后是一种自适应方案

```python
pi_logratios = chosen_policy_log_probs - reject_policy_log_probs
ref_logratios = chosen_ref_log_probs - reject_ref_log_probs
logits = pi_logratios - ref_logratios
```

如果
ref_logratios大于pi_logratios，证明模型质量变差，还有没原始参考的好，如果pi_logratios大于ref_logratios，证明模型的区分能力有所上升。



### 关于mask

SFT 中

`loss_mask` 通常表示：

```
只在回答部分计算监督学习损失，
prompt 和 padding 不计算。
```

例如：

```
prompt token → 0
answer token → 1
padding token → 0
```

------

DPO 中

`mask` 同样只保留 answer 区域：

```
ref_log_probs = (ref_log_probs * mask).sum(dim=1)
policy_log_probs = (policy_log_probs * mask).sum(dim=1)
```

但用途变成了：

```
统计 chosen 回答整体概率；
统计 rejected 回答整体概率；
然后比较两者。
```

所以：

| 阶段 | mask 的作用                                    |
| ---- | ---------------------------------------------- |
| SFT  | 只让标准答案 token 参与交叉熵                  |
| DPO  | 只让回答 token 参与 chosen / rejected 概率比较 |





DPO从STF的结果开始的，学习率通常比SFT更小
如果学习率过大，可能会

遗忘已有知识；
语言能力下降；
过度迎合偏好数据。





一张表总结 SFT 与当前 DPO 代码差异

| 对比项     | SFT                  | 当前 DPO 脚本                   |
| ---------- | -------------------- | ------------------------------- |
| 数据形式   | `prompt + 标准答案`  | `prompt + chosen + rejected`    |
| 数据集类   | SFT 数据集           | `DPODataset`                    |
| 模型数量   | 一个训练模型         | `model` + 冻结的 `ref_model`    |
| 初始化权重 | 通常从 pretrain 开始 | 默认从 `full_sft` 开始          |
| 输入 batch | `[B, L]`             | 拼接后 `[2B, L]`                |
| 前向传播   | 模型一次前向         | policy 与 reference 分别前向    |
| 损失函数   | token 交叉熵         | `dpo_loss()`                    |
| mask 作用  | 只监督答案 token     | 只累计 chosen/rejected 回答概率 |
| 学习目标   | 学会复现答案         | 更偏好 chosen，而不是 rejected  |
| 特有参数   | 无 `beta`            | 新增 `beta=0.15`                |
| 学习率     | 通常相对较大         | 默认 `4e-8`，更小               |
| 保存结果   | `full_sft_768.pth`   | `dpo_768.pth`                   |



ok，老规矩，难度和sft差不多。个人感觉是因为vocab size 过大，通过这种方式，显示增强某些token的分布概率。

---

## Day 7：PPO / RLAIF / Agentic RL 概览

* **任务**：理解高级训练阶段
* **操作**：

  1. 查看 MiniMind 提供的 PPO/GRPO/CISPO 脚本
  2. 读懂 reward calculation、policy update 流程
  3. 尝试小样本训练 pipeline，观察 reward loss
  4. 理解 Tool Use 和 Agentic RL 数据流
  
  

---

## Day 8–10：综合训练实验与日志整理

* **任务**：完成一个小训练闭环，整合前几天知识
* **操作**：

  1. 从 pretrain → SFT → LoRA → DPO 完整跑一次小样本训练
  2. 记录每个阶段的 loss、显存、速度、输出质量
  3. 绘制训练曲线、参数量变化表
  4. 撰写小报告，总结每个阶段关键原理和超参数影响

