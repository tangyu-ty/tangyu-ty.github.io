---
layout: post
title: 论文项目02
date: 2026-03-10 16:10 +0800
description: demo描述
pin: true
math: true
mermaid: true
category:
- 论文思路
tags:
- 论文思路
published: true
sitemap: false
author: tangyu
---
这个博客用来记录我的论文思路2的研究进展

## 26.03.10
项目01暂时搁置，我这边打算用torch.nn的组件搭建一个decoderonly的架构，改变传统encoder-decoder的模式，一方面可以学习到神经网络的最新进展，另一方面可以训练自己算法底层的能力。


## 26.03.12
修改一下思路

我认为，以前是01二进制验证的，后面是通过byte字节校验，后面是char校验，所以在大模型时代，大模型之间的通信的最小单位应当是token，作为语义通信，所以得以token作为最小单元，进行校验，如果一个token乱码，应当通过其他token校验得到这个token。

例子，比如说这3个token
i am eating 

应当编码为

2-6个token，然后传输，传输完成后恢复成原来的3个token

目的：保证语义最小的信息语义完整度，同时删去冗余的信息。自适应根据snr传输不同数量级的token




## 26-03-12

存在的问题
1、直接用别人的token，不太好，可能数据分布不均衡，不过先跑通了再说
2、考虑终止符。模型的forward有问题


## 26-03-13

已经能够跑通了，不过有个问题，不过仍是以前的代码上改的

自己写一份完整的还是够呛

现在考虑的是，

并不是token级别的通信，这个比较麻烦，主要是在tx端不太好循环生成，目前正在想办法，尽量实现循环生成的方式。


ok双decoder-only的架构跑通了，先看看实验结果把，看完了再去试试token级别的通信，或许会更好，不过可能要写一个token的循环和解码器，有点不利于训练。因为再tx端要生成一个序列，在rx端也要生成一个序列，不太好。


## 26-03-16

实验结果并不理想，仔细检查了下，发现是之前的细节有些问题，处理下，应该会好一点吧，希望指标能更好点


另外，完成了token级别的语义通信，目前还没测试。


## 26-03-20

很不顺利，原来的代码修修改改，成屎山了，我想了想还是用trae重构了整个项目，目前看起来顺眼多了，基本上面向对象，拓展性、可读性提高一大截，耦合性也降低了。
日志和其他体系都很好。


## 26-03-23

最近被之前的项目耽搁了，目前的情况就是基本上实现了所有功能，不过细节还需要打磨，目前完成了

## 项目结构

```
项目2/
├── config.py          # 配置管理模块
├── dataload.py        # 数据流管道
├── evaluate.py        # 测试与评估
├── logger.py          # 训练监控与日志系统
├── main.py            # 主入口
├── model.py           # 模型构建与信道融合
├── requirements.txt   # 依赖包列表
├── trainer.py         # 训练与验证引擎
└── utils.py           # 工具函数模块
```

这两天完善了，评估指标

bleu、相似度、bert f1等指标均已实现。

未来主要处理channel这个问题。

我这里处理不太好。信道做的太糟糕。


但是看了gensc和deepsc的发现，有细微的差别，这个差别有影响吗？目前在探索。

准备再看看计算机视觉的



